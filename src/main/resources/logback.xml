<?xml version="1.0" encoding="UTF-8"?>
<configuration
    xmlns="http://ch.qos.logback/xml/ns/logback"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://ch.qos.logback/xml/ns/logback https://raw.githubusercontent.com/enricopulatzo/logback-XSD/master/src/main/xsd/logback.xsd">
    <property name="LOG_HOME" value="D:/data/websocket/logs"/>
    <!-- 输出到控制台 -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender" >
        <!-- 输出的格式 -->
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{50}[%L]:  %msg%n</pattern>
        </encoder>
    </appender>
    
     <appender name="DEBUGFILE" class="ch.qos.logback.core.rolling.RollingFileAppender" >
     	<filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level >debug</level>
            <onMatch>ACCEPT</onMatch>  <!-- 如果命中就使用这条规则 -->
            <onMismatch>DENY</onMismatch>  <!-- 如果没有命中就 禁止这条日志-->
        </filter>
        <file>${LOG_HOME}/logback_debug.log</file>
        <!-- 配置滚动的策略 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy" >
            <!-- 日志名称的格式 -->
            <fileNamePattern>${LOG_HOME}/logback_debug-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>5MB</maxFileSize>      
            <maxHistory>60</maxHistory>  
            <totalSizeCap>200MB</totalSizeCap>  
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{50}[%L]: %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="INFOFILE" class="ch.qos.logback.core.rolling.RollingFileAppender" >
     	<filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level >info</level>
            <onMatch>ACCEPT</onMatch>  <!-- 如果命中就使用这条规则 -->
            <onMismatch>DENY</onMismatch>  <!-- 如果没有命中就 禁止这条日志-->
        </filter>
        <file>${LOG_HOME}/logback_info.log</file>
        <!-- 配置滚动的策略 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy" >
            <!-- 日志名称的格式 -->
            <fileNamePattern>${LOG_HOME}/logback_info-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>5MB</maxFileSize>      
            <maxHistory>60</maxHistory>  
            <totalSizeCap>200MB</totalSizeCap>  
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{50}[%L]:  %msg%n</pattern>
        </encoder>
    </appender>
    
    <appender name="ERRORFILE" class="ch.qos.logback.core.rolling.RollingFileAppender" >
     	<filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level >error</level>
            <onMatch>ACCEPT</onMatch>  <!-- 如果命中就使用这条规则 -->
            <onMismatch>DENY</onMismatch>  <!-- 如果没有命中就 禁止这条日志-->
        </filter>
        <file>${LOG_HOME}/logback_error.log</file>
        <!-- 配置滚动的策略 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy" >
            <!-- 日志名称的格式 -->
            <fileNamePattern>${LOG_HOME}/logback_error-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>5MB</maxFileSize>      
            <maxHistory>60</maxHistory>  
            <totalSizeCap>200MB</totalSizeCap>  
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{50}[%L]: %n %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.test" additivity="false" level="debug" >
         <appender-ref ref="INFOFILE" />
         <appender-ref ref="ERRORFILE" />
         <appender-ref ref="DEBUGFILE" />
    </logger>

    <!-- 相当于logger元素，只是name值已经确定为root了 -->
    <root level="debug" >
        <!-- -->
        <appender-ref ref="STDOUT"  />  
        <appender-ref ref="INFOFILE" />
        <appender-ref ref="ERRORFILE" />
        <appender-ref ref="DEBUGFILE" />
    </root>
</configuration>